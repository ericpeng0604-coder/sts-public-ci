name: STS1 Phase 1 Anger Pommel Composition V2 Holdout

on:
  push:
    branches:
      - gpt/sts1-phase1-anger-pommel-composition-v2-holdout
    paths:
      - '.github/workflows/sts1-phase1-anger-pommel-composition-v2-holdout.yml'
      - 'tests/data/sts1_phase1_anger_pommel_composition_v2_holdout_seeds_256.txt'
      - 'scripts/sts1/generate_phase1_anger_pommel_composition_v2_holdout.py'
      - 'scripts/sts1/run_phase1_anger_pommel_composition_v2_holdout_quality.py'
      - 'src/roguelike_ai/sts1_teacher/anger_pommel_composition_policy_v2.py'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  composition-v2-holdout:
    runs-on: ubuntu-latest
    timeout-minutes: 120
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 2

      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Freeze V2 and 520xxx holdout before quality
        run: |
          python -m py_compile \
            scripts/sts1/generate_phase1_anger_pommel_composition_v2_holdout.py \
            scripts/sts1/run_phase1_anger_pommel_composition_v2_holdout_quality.py \
            src/roguelike_ai/sts1_teacher/anger_pommel_composition_policy_v2.py
          python - <<'PY'
          from pathlib import Path
          import hashlib
          p = Path('tests/data/sts1_phase1_anger_pommel_composition_v2_holdout_seeds_256.txt')
          seeds = [int(x.strip()) for x in p.read_text().splitlines() if x.strip() and not x.lstrip().startswith('#')]
          assert seeds == list(range(520001, 520257))
          print('ANGER_POMMEL_V2_HOLDOUT_SEED_FREEZE = PASS')
          print('SOURCE_SEED_COUNT=256')
          print('SOURCE_SEED_FILE_SHA256=' + hashlib.sha256(p.read_bytes()).hexdigest())
          print('V2_POLICY=jaw-worm-turn1-anger-pommel-search-top-set-v2')
          print('QUALITY_GATE=anger-pommel-composition-v2-holdout-ge-search-gt-simple-random')
          print('SEARCH_CONFIG=samples8-rollout256-node16384-depth30-seed20260829')
          print('ORACLE_CONFIG=samples4-mcts2000-per-action-per-sample')
          print('QUALITY_RESULTS_OBSERVED_BEFORE_FREEZE=0')
          print('PRIOR_500XXX_QUALITY_ROWS_READ=0')
          PY
          git diff --check HEAD^

      - name: Hydrate exact upstream sources
        run: python scripts/sts1/hydrate_jialeiv_baseline_v0.py

      - name: Apply diagnostic oracle binding
        run: python scripts/sts1/apply_phase1_oracle_judge_overlay.py

      - name: Apply read-only public-state overlay
        run: python scripts/sts1/apply_phase1_public_state_overlay.py

      - name: Apply public BattleContext reconstruction overlay
        run: python scripts/sts1/apply_phase1_public_reconstruction_overlay.py

      - name: Apply audited turn-aware history overlay
        run: python scripts/sts1/apply_phase1_public_history_guard_overlay.py

      - name: Apply established starter midturn counters
        run: python scripts/sts1/apply_phase1_public_midturn_counter_overlay.py

      - name: Apply CommunicationMod card aliases
        run: python scripts/sts1/apply_phase1_public_communicationmod_card_alias_overlay.py

      - name: Apply audited rich-card auxiliary path
        run: python scripts/sts1/apply_phase1_public_draw_midturn_aux_overlay.py

      - name: Build native Python binding
        run: python scripts/sts1/build_probe.py

      - name: Generate blind 520xxx holdout suite twice
        run: |
          python scripts/sts1/generate_phase1_anger_pommel_composition_v2_holdout.py --output evidence/anger-pommel-v2-holdout-a.json
          python scripts/sts1/generate_phase1_anger_pommel_composition_v2_holdout.py --output evidence/anger-pommel-v2-holdout-b.json
          cmp evidence/anger-pommel-v2-holdout-a.json evidence/anger-pommel-v2-holdout-b.json
          cp evidence/anger-pommel-v2-holdout-a.json evidence/anger-pommel-v2-holdout-suite.json

      - name: Run unchanged predeclared V2 holdout gate twice
        run: >-
          python scripts/sts1/run_phase1_anger_pommel_composition_v2_holdout_quality.py
          --suite evidence/anger-pommel-v2-holdout-suite.json
          --output-dir evidence/sts1/phase1_anger_pommel_composition_v2_holdout

      - name: Show V2 holdout summary
        if: always()
        run: |
          test -f evidence/sts1/phase1_anger_pommel_composition_v2_holdout/summary.json && cat evidence/sts1/phase1_anger_pommel_composition_v2_holdout/summary.json || true
          test -f evidence/anger-pommel-v2-holdout-suite.json && python - <<'PY'
          from pathlib import Path
          import json
          p = Path('evidence/anger-pommel-v2-holdout-suite.json')
          if p.exists():
              s = json.loads(p.read_text())
              print('SAMPLING_CONTRACT=' + s['sampling_contract_version'])
              print('SUITE_DIGEST=' + s['suite_digest'])
              print('CASE_COUNT=' + str(s['case_count']))
              print('REJECTED_COUNTS=' + str(s['rejected_counts']))
              print('PRIOR_500XXX_QUALITY_ROWS_READ=' + str(s['prior_500xxx_quality_rows_read']))
              print('PRIOR_510XXX_DISCOVERY_ROWS_USED_FOR_SELECTION=' + str(s['prior_510xxx_discovery_rows_used_for_selection']))
          PY

      - name: Upload V2 holdout evidence
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: sts1-phase1-anger-pommel-composition-v2-holdout-evidence
          path: |
            evidence/anger-pommel-v2-holdout-suite.json
            evidence/sts1/phase1_anger_pommel_composition_v2_holdout/
          if-no-files-found: warn
          retention-days: 90
