name: STS1 Phase 1 Blue Slaver Frozen Quality

on:
  push:
    branches:
      - gpt/sts1-phase1-blue-slaver-expansion
    paths:
      - '.github/workflows/sts1-phase1-blue-slaver-quality.yml'
      - 'tests/data/sts1_phase1_public_search_v1_blue_slaver_source_seeds_64.txt'
      - 'scripts/sts1/generate_phase1_public_search_v1_blue_slaver_suite.py'
      - 'scripts/sts1/run_phase1_public_search_v1_blue_slaver_quality.py'
      - 'src/roguelike_ai/sts1_teacher/**'
      - 'scripts/sts1/apply_phase1_public_state_overlay.py'
      - 'scripts/sts1/apply_phase1_public_reconstruction_overlay.py'
      - 'scripts/sts1/apply_phase1_public_history_guard_overlay.py'
      - 'scripts/sts1/apply_phase1_oracle_judge_overlay.py'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  blue-slaver-v1-quality:
    runs-on: ubuntu-latest
    timeout-minutes: 120
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 1

      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Freeze contract and syntax
        run: |
          python -m py_compile \
            scripts/sts1/generate_phase1_public_search_v1_blue_slaver_suite.py \
            scripts/sts1/run_phase1_public_search_v1_blue_slaver_quality.py
          python - <<'PY'
          from pathlib import Path
          import hashlib
          p = Path('tests/data/sts1_phase1_public_search_v1_blue_slaver_source_seeds_64.txt')
          seeds = [int(x.strip()) for x in p.read_text().splitlines() if x.strip() and not x.lstrip().startswith('#')]
          assert len(seeds) == 64 and len(set(seeds)) == 64
          assert min(seeds) == 390001 and max(seeds) == 390064
          print('BLUE_SLAVER_SOURCE_SEED_FREEZE = PASS')
          print('SOURCE_SEED_FILE_SHA256=' + hashlib.sha256(p.read_bytes()).hexdigest())
          PY

      - name: Hydrate exact upstream sources
        run: python scripts/sts1/hydrate_jialeiv_baseline_v0.py

      - name: Apply diagnostic oracle binding
        run: python scripts/sts1/apply_phase1_oracle_judge_overlay.py

      - name: Apply read-only public-state overlay
        run: python scripts/sts1/apply_phase1_public_state_overlay.py

      - name: Apply public BattleContext reconstruction overlay
        run: python scripts/sts1/apply_phase1_public_reconstruction_overlay.py

      - name: Apply audited opening-enemy overlay
        run: python scripts/sts1/apply_phase1_public_history_guard_overlay.py

      - name: Build native Python binding
        run: python scripts/sts1/build_probe.py

      - name: Generate frozen Blue Slaver suite twice
        run: |
          python scripts/sts1/generate_phase1_public_search_v1_blue_slaver_suite.py --output evidence/blue-slaver-freeze-a.json
          python scripts/sts1/generate_phase1_public_search_v1_blue_slaver_suite.py --output evidence/blue-slaver-freeze-b.json
          cmp evidence/blue-slaver-freeze-a.json evidence/blue-slaver-freeze-b.json
          cp evidence/blue-slaver-freeze-a.json evidence/blue-slaver-suite.json

      - name: Run two-repeat Blue Slaver quality benchmark
        run: >-
          python scripts/sts1/run_phase1_public_search_v1_blue_slaver_quality.py
          --suite evidence/blue-slaver-suite.json
          --output-dir evidence/sts1/phase1_public_search_v1_blue_slaver_quality

      - name: Show quality summary
        if: always()
        run: test -f evidence/sts1/phase1_public_search_v1_blue_slaver_quality/summary.json && cat evidence/sts1/phase1_public_search_v1_blue_slaver_quality/summary.json || true

      - name: Upload quality evidence
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: sts1-phase1-public-search-v1-blue-slaver-quality-evidence
          path: evidence/sts1/phase1_public_search_v1_blue_slaver_quality/
          if-no-files-found: warn
          retention-days: 90
