# STS Public CI

This repository contains only the minimal allowlisted code needed to run public CI benchmarks.
The main development repository remains private.

Private Git history, datasets, model artifacts, credentials, local configuration, and unrelated project code are intentionally excluded.
